{
  "name": "connecting-websockets-to-osc-for-rpi",
  "version": "1.0.0",
  "description": "Setup for creating a server, connecting via websockets and sending osc on Raspberry Pi",
  "main": "webserver.js",
  "scripts": {
    "start": "node webserver.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Guergana Tzatchkova",
  "license": "ISC",
  "dependencies": {
    "osc-js": "^2.1.0",
    "socket.io": "^2.3.0"
  }
}
